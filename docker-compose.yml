services:
  wima:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - wima-data:/app/data
    environment:
      - DATABASE_URL=file:/app/data/wima.db
      - API_SECRET=${API_SECRET:-change-me}
      - PORT=3000
    restart: unless-stopped

volumes:
  wima-data:
